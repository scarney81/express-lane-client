extends layout

block content
  .container
    h1 Shopping Cart
    if products.length
      ul
        each product in products
          li #{product.id} x#{product.quantity}
    else
      p: em Your shopping cart is empty.
