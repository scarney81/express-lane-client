extends layout

block content
  .container
    h1 Orders

    if orders.length
      ul
        each order in orders
          li #{order.total_price}: #{order.status}
    else
      p: em You have not placed any orders.

    div.buttons
      a.btn(href='/') Continue Shopping
